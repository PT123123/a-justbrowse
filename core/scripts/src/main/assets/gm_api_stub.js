// GM API stub — 用于在页面加载前注入，避免脚本因 GM_* 未定义报错。
// 完整实现由 ScriptInjector.wrapScript 根据 @grant 动态生成。
window.GM = window.GM || {};
window.GM_info = window.GM_info || { script: { name: 'JustBrowse stub' }, version: '0.1' };
